<!DOCTYPE html>
<html lang="{{ locale }}">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
  <title>{{title}}</title>
  <meta name="description" content="{{description}}">
  {% set hreflang = "en" if locale === "zh" else "zh" %}
  {% set hreflangHref = site.absoluteURL + "/en" + permalinkMin if locale === "zh" else site.absoluteURL + "/zh" + permalinkMin %}
  <link rel="alternate" hreflang="{{hreflang}}" href="{{hreflangHref}}" />
  <link rel="shortcut icon" href="/assets/favicon.ico" type="image/x-icon">`  
  <link rel="stylesheet" href="/assets/css/style.css">
</head>
<body>
  language switcher
  {% include "partials/lggnav.njk" %}


  main navigation
  {% include "partials/mainnav.njk" %}

  {% block content %}{% endblock %}

  {% include "partials/footer.njk" %}

</body>
</html>
